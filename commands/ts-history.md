---
description: Show recent TokenSpeed request history
---

Run the `ts-history` tool exactly once.

If the user included a positive integer argument in the slash command, pass it as `limit`.
Otherwise call without arguments.

Return only the tool result.
